#include <stdio.h>

// program to run a loop from 10 to 0
int main() {
	int i =10;
	long long unsigned int t = 10;
	/* this should be ignored*/while(i>=0) {
		printf("Current value of i: %d", i); // print statement
	}
	int a@b =5; //error
    int x = 045; // octal
    int y = 078; // error in octal
    int z = 0x7b; // hexadecimal
    int w = 0x6j; // should be an error
}
